<Window x:Class="DatasetManager.App.Views.AnnotationEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="标注批次" Height="470" Width="600" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="#F7F8FA">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="登记标注批次" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="批次名称 *" Margin="0,0,0,6"/>
            <TextBox x:Name="NameBox" ToolTip="例如：第一轮标注、规则V2复核"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Text="LabelMe 标签目录 *" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox"/>
                <Button Grid.Column="1" Content="浏览…" Click="Browse_Click" Margin="8,0,0,0"/>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,0,0,14">
            <TextBlock Text="本轮标注规则与备注" Margin="0,0,0,6"/>
            <TextBox x:Name="NotesBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="110"/>
        </StackPanel>
        <TextBlock x:Name="ErrorText" Grid.Row="4" Foreground="#DC2626" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" MinWidth="88"/>
            <Button Content="保存并统计" Click="Save_Click" MinWidth="108" Background="#2563EB" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
