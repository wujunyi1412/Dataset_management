<Window x:Class="DatasetManager.App.Views.DatasetEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据集信息" Height="600" Width="620" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="#F7F8FA">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="登记数据集" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>
        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="名称 *" Margin="0,0,0,6"/>
                <TextBox x:Name="NameBox"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="类型 *" Margin="0,0,0,6"/>
                <ComboBox x:Name="TypeBox" Padding="7" DisplayMemberPath="Name" SelectedValuePath="Value"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Text="数据集目录 *" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox"/>
                <Button Grid.Column="1" Content="浏览…" Click="Browse_Click" Margin="8,0,0,0"/>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,0,0,14">
            <TextBlock Text="来源数据集（派生关系）" Margin="0,0,0,6"/>
            <ComboBox x:Name="ParentBox" Padding="7" DisplayMemberPath="Name" SelectedValuePath="Id"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Margin="0,0,0,14">
            <TextBlock Text="长期备注" Margin="0,0,0,6"/>
            <TextBox x:Name="NotesBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="100"/>
        </StackPanel>
        <StackPanel Grid.Row="5" Margin="0,0,0,14">
            <TextBlock Text="本次修改说明" Margin="0,0,0,6"/>
            <TextBox x:Name="ChangeBox" ToolTip="会追加到数据集的修改记录中"/>
        </StackPanel>
        <TextBlock x:Name="ErrorText" Grid.Row="6" Foreground="#DC2626" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" MinWidth="88"/>
            <Button Content="保存" Click="Save_Click" MinWidth="88" Background="#2563EB" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
