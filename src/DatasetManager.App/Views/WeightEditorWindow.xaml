<Window x:Class="DatasetManager.App.Views.WeightEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="权重记录" Height="550" Width="640" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="#F7F8FA" Icon="../Assets/DatasetManager.ico">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="登记权重信息" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="权重名称 *" Margin="0,0,0,6"/>
            <TextBox x:Name="NameBox" ToolTip="例如：缺陷检测 V3、最佳验证集权重"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Text="权重文件 *" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" TextChanged="PathBox_TextChanged"/>
                <Button Grid.Column="1" Content="浏览…" Click="Browse_Click" Margin="8,0,0,0"/>
            </Grid>
            <TextBlock x:Name="FormatText" Foreground="#6B7280" Margin="0,5,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,0,0,14">
            <TextBlock Text="备注" Margin="0,0,0,6"/>
            <TextBox x:Name="NotesBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="120"/>
        </StackPanel>
        <StackPanel x:Name="ChangePanel" Grid.Row="4" Margin="0,0,0,14">
            <TextBlock Text="本次修改说明" Margin="0,0,0,6"/>
            <TextBox x:Name="ChangeBox" ToolTip="例如：替换为 ONNX 导出版本、补充验证结果"/>
        </StackPanel>
        <TextBlock x:Name="ErrorText" Grid.Row="5" Foreground="#DC2626" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" MinWidth="88"/>
            <Button Content="保存" Click="Save_Click" MinWidth="96" Background="#2563EB" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
